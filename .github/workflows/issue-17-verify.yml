name: Issue 17 Verification

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - agent/restore-gamedig-query-port-resolution
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: 1.3.14

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Show formatter diff
        run: |
          ./node_modules/.bin/oxfmt src/container/gamedig/errors.ts src/container/query-params.ts
          git diff -- src/container/gamedig/errors.ts src/container/query-params.ts
          git checkout -- src/container/gamedig/errors.ts src/container/query-params.ts

      - name: Typecheck
        run: bun run typecheck

      - name: Check
        run: bun run check

      - name: Test
        run: bun test

      - name: Build Docker image
        run: docker build .
